# GERADO por tests/trigger-eval/gen-eval-cases.sh — nao edite a mao.
# Fonte: plugins/cstk/skills/*/evals/triggers.jsonl + tests/trigger-eval/negatives.jsonl
schema_version: "1.0"
name: "model-selector-001"
description: "trigger eval: a query deve disparar a skill model-selector"
tags: ["trigger", "model-selector", "base"]
execution:
  prompt: "essa tarefa ai e complexa o suficiente pra valer opus ou da pra rodar em haiku mesmo?"
  max_turns: 3
  timeout_seconds: 120
runs: 1
graders:
  - type: tool_used
    name: "fires-model-selector"
    tool: "Skill"
    input_match: "model-selector"
    min: 1
