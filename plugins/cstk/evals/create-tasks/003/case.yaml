# GERADO por tests/trigger-eval/gen-eval-cases.sh — nao edite a mao.
# Fonte: plugins/cstk/skills/*/evals/triggers.jsonl + tests/trigger-eval/negatives.jsonl
schema_version: "1.0"
name: "create-tasks-003"
description: "trigger eval: a query deve disparar a skill create-tasks"
tags: ["trigger", "create-tasks", "hard"]
execution:
  prompt: "essa feature ainda nao tem nenhum item planejado; preciso transformar o escopo em pedacos executaveis com a ordem certa e o que depende do que"
  max_turns: 3
  timeout_seconds: 120
runs: 1
graders:
  - type: tool_used
    name: "fires-create-tasks"
    tool: "Skill"
    input_match: "create-tasks"
    min: 1
