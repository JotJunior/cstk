# GERADO por tests/trigger-eval/gen-eval-cases.sh — nao edite a mao.
# Fonte: plugins/cstk/skills/*/evals/triggers.jsonl + tests/trigger-eval/negatives.jsonl
schema_version: "1.0"
name: "specify-002"
description: "trigger eval: a query deve disparar a skill specify"
tags: ["trigger", "specify", "base"]
execution:
  prompt: "transforma essa descricao em linguagem natural numa spec SDD com user stories e FRs"
  max_turns: 3
  timeout_seconds: 120
runs: 1
graders:
  - type: tool_used
    name: "fires-specify"
    tool: "Skill"
    input_match: "specify"
    min: 1
