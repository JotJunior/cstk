# GERADO por tests/trigger-eval/gen-eval-cases.sh — nao edite a mao.
# Fonte: plugins/cstk/skills/*/evals/triggers.jsonl + tests/trigger-eval/negatives.jsonl
schema_version: "1.0"
name: "validate-documentation-003"
description: "trigger eval: a query deve disparar a skill validate-documentation"
tags: ["trigger", "validate-documentation", "gen"]
execution:
  prompt: "o nosso UC-CAD-003 tem todos os campos que precisa? quero saber se ta faltando alguma seção obrigatoria no documento"
  max_turns: 3
  timeout_seconds: 120
runs: 1
graders:
  - type: tool_used
    name: "fires-validate-documentation"
    tool: "Skill"
    input_match: "validate-documentation"
    min: 1
