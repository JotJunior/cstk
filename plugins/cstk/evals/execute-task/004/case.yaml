# GERADO por tests/trigger-eval/gen-eval-cases.sh — nao edite a mao.
# Fonte: plugins/cstk/skills/*/evals/triggers.jsonl + tests/trigger-eval/negatives.jsonl
schema_version: "1.0"
name: "execute-task-004"
description: "trigger eval: a query deve disparar a skill execute-task"
tags: ["trigger", "execute-task", "gen"]
execution:
  prompt: "o backlog esta montado e a proxima tarefa e criar a migracao do banco para adicionar a coluna de status — como eu executo isso dentro do fluxo do projeto?"
  max_turns: 3
  timeout_seconds: 120
runs: 1
graders:
  - type: tool_used
    name: "fires-execute-task"
    tool: "Skill"
    input_match: "execute-task"
    min: 1
