# GERADO por tests/trigger-eval/gen-eval-cases.sh — nao edite a mao.
# Fonte: plugins/cstk/skills/*/evals/triggers.jsonl + tests/trigger-eval/negatives.jsonl
schema_version: "1.0"
name: "converge-003"
description: "trigger eval: a query deve disparar a skill converge"
tags: ["trigger", "converge", "base"]
execution:
  prompt: "acha os gaps entre o que foi especificado nessa feature e o que o codigo realmente faz hoje"
  max_turns: 3
  timeout_seconds: 120
runs: 1
graders:
  - type: tool_used
    name: "fires-converge"
    tool: "Skill"
    input_match: "converge"
    min: 1
