# SCHEMA-MIGRATION Checklist: otel-model-breakdown

**Purpose**: Quality gate dos requisitos (spec/plan/data-model/quickstart) antes de `/create-tasks`,
com foco em 4 riscos levantados na retomada desta onda: compatibilidade cross-repo com o
cstk-panel (R1), invariante NULL-vs-zero (FR-004/Principio VI), disciplina de edicao das 12
asercoes de versao nos testes (R2), e fronteira de seguranca do isolamento de `knowledge_fts`
(LLM01/ASI06).
**Created**: 2026-07-28
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md) | [data-model.md](../data-model.md) | [quickstart.md](../quickstart.md)

## Compatibilidade Cross-Repo (R1 — bump quebra o cstk-panel instalado)

- [ ] CHK001 - Os Requisitos Funcionais (FR-001..FR-009) incluem algum requisito explicito sobre o comportamento do cstk-panel apos o bump v11->v12, alem da nota de risco registrada no plan? [Completude, Spec §Requirements vs Plan §R1] {auto} `[Gap]` — nao ha. FR-001..FR-009 cobrem apenas persistencia/NULL/idempotencia/confinamento de deps; nenhum FR menciona o painel ou compatibilidade cross-repo. O unico lugar onde R1 existe e `plan.md §"Riscos e Dependencias de Release"`, que e nota de risco de planejamento, nao requisito rastreavel pela spec.
- [ ] CHK002 - Existe Success Criteria ou Acceptance Scenario mensuravel para o estado esperado do painel ANTES do fix definitivo ser publicado no `cstk-panel` (ex.: "com `CSTK_SCHEMA_VERSIONS` setado, o painel continua servindo todas as rotas")? [Mensurabilidade, Spec §Success Criteria] {auto} `[Gap]` — SC-001..SC-004 cobrem apenas exatidao de valores, idempotencia de reindex e suite verde; nenhum SC cobre o comportamento do painel/mitigacao. O workaround (`CSTK_SCHEMA_VERSIONS=...`) e descrito em `plan.md` mas nunca como criterio de aceite testavel.
- [ ] CHK003 - O plano diz que o fix definitivo (`DEFAULT_SCHEMA_VERSIONS` += `'12'` no repo `cstk-panel`) "deve ser agendado junto do release do cstk" — ha rastreabilidade formal (issue aberta, sugestao registrada via `suggestions.sh`) garantindo que esse agendamento nao se perca apos o merge desta feature? [Dependencias e Premissas, Plan §R1] {auto} `[Gap]` — nenhum artefato desta feature referencia numero de issue ou sugestao registrada; o compromisso de agendamento existe apenas como prosa no plan.
- [x] CHK004 - A decisao de NAO bloquear esta feature pelo R1 (painel tratado como consumidor downstream opcional) reflete o apetite de risco aceito pelo mantenedor para quebrar um consumidor instalado sem coordenacao previa de release? [Risco] {humano} — **RESOLVIDO (2026-07-28, mantenedor)**: NAO. O risco NAO e aceito sem coordenacao. Decisao: coordenar release do repo `cstk-panel` (bump da allowlist `DEFAULT_SCHEMA_VERSIONS` em `apps/server/src/config.ts:31`, hoje `['2'..'11']`) antes ou junto do merge desta feature. Vira task explicita no backlog de `create-tasks`. A mitigacao por env `CSTK_SCHEMA_VERSIONS` permanece como paliativo documentado, nao como solucao.

## Invariante NULL-vs-Zero (FR-004 / Principio VI — zero fabricacao)

- [x] CHK005 - FR-004 define, em linguagem MUST, que ausencia de dado OTel resulta em NULL e nunca em zero fabricado? [Clareza, Spec §FR-004] {auto} — Satisfeito: "Ausencia de dado OTel ... MUST resultar em valor NULL ... jamais um valor zero fabricado quando o dado simplesmente nao foi medido" (spec.md FR-004).
- [ ] CHK006 - Ha ao menos um Acceptance Scenario ou cenario de quickstart que demonstre, contra dado REAL do corpus, que um valor `0` legitimamente medido e preservado como `0` (nao confundido com ausencia)? [Cobertura de Edge Cases, Mensurabilidade] {auto} `[Gap]` — quickstart.md Cenarios 3 e 5 testam exclusivamente o lado da ausencia (`otel_main_*` = NULL, zero linhas em `wave_model_usage`); nenhum dos 9 cenarios exercita um `0` real preservado, apesar de o corpus conhecido ja ter esse caso (`mcp-project-scafold`, onda com `otel_cost_main_usd = 0.0`). A garantia de que `0` sobrevive existe so como narrativa de design em `data-model.md §Regra transversal: NULL vs zero`, sem cenario de verificacao.
- [x] CHK007 - O mecanismo tecnico que garante a distincao NULL-vs-zero (operador `//` do jq preserva `0`; `recall_int_or_null`/`recall_real_or_null` convertem so string vazia/nao-numerica em NULL) esta documentado com referencia a linha de codigo verificavel? [Clareza, Data-model §Regra transversal NULL vs zero] {auto} — Satisfeito: `data-model.md` linhas 104-110 citam `recall.sh:850` (`recall_int_or_null`) e `recall.sh:862` (`recall_real_or_null`) e o comportamento do `//` do jq.
- [x] CHK008 - O caso onde `by_source` contem apenas uma das duas fontes (`main` ou `subagent`) esta coberto como Acceptance Scenario, garantindo que a fonte ausente fique NULL enquanto a presente e avaliada normalmente (inclusive se seu valor for `0`)? [Cobertura de Edge Cases, Spec §US2 Acceptance Scenario 2] {auto} — Satisfeito: spec.md linhas 74-77 ("by_source so tem a chave subagent ... colunas de breakdown do main ficam NULL nessa linha").

## Disciplina de Edicao das 12 Asercoes de Versao (R2)

- [x] CHK009 - O plano identifica precisamente as 12 linhas de teste que comparam `schema_version` contra `"11"` (numeros de linha explicitos), distinguindo a edicao dirigida de um `sed` global perigoso? [Clareza, Plan §R2] {auto} — Satisfeito: `plan.md §R2` lista as 12 linhas (`611, 656, 678, 1879, 2195, 2285, 3035, 3161, 3216, 3265, 3337, 3474`) e adverte contra `sed` global (atingiria numeros/textos nao relacionados).
- [x] CHK010 - Ha um requisito ou criterio de aceite (nao so uma nota de risco) exigindo que a migracao dessas 12 asercoes ocorra no MESMO commit do bump de `RECALL_SCHEMA_VERSION`, para nao deixar a suite quebrada entre commits? [Consistencia, Spec §SC-004] {auto} — Satisfeito: `quickstart.md` Cenario 9 diz explicitamente "precisam ir para \"12\" no mesmo commit do bump, senao a suite quebra"; SC-004 exige suite 100% verde apos a migracao.
- [x] CHK011 - A armadilha de uma das 12 mensagens de assercao ja estar dessincronizada do valor comparado esta identificada com linha exata e correcao esperada, para nao ser perdida na edicao linha a linha? [Completude, Rastreabilidade, Research §Decision 9] {auto} — Satisfeito: `research.md` Decision 9 identifica a linha `678` ("mensagem de falha desatualizada — 'esperado 10 apos 2x' — enquanto compara contra 11") e instrui corrigir a mensagem junto do bump.

## Fronteira de Seguranca — Isolamento de `knowledge_fts` (LLM01/ASI06)

- [x] CHK012 - O plano declara, em termos verificaveis, que `wave_model_usage` NAO deve alimentar `knowledge_fts`? [Clareza, Plan §Tratamento do nome do modelo] {auto} — Satisfeito: `plan.md` item "1.ter" declara explicitamente que nenhuma tabela de metrica alimenta o FTS e que "a implementacao NAO deve adicionar `wave_model_usage` ao `knowledge_fts`".
- [ ] CHK013 - Ha um cenario de teste (quickstart.md ou tasks.md) que verifique programaticamente a ausencia de `wave_model_usage`/label de modelo em `knowledge_fts` (ex.: `SELECT count(*) FROM knowledge_fts WHERE type='wave_model_usage'` retorna 0), ou essa garantia depende apenas de uma instrucao de implementacao nunca escrever o codigo que a violaria? [Mensurabilidade, Verificabilidade] {auto} `[Gap]` — nenhum dos 9 cenarios do quickstart exercita esta fronteira; a garantia hoje e puramente uma instrucao textual do plan ("a implementacao NAO deve..."), sem teste automatizado que falharia caso alguem adicione o tipo ao FTS no futuro.
- [ ] CHK014 - O isolamento de `wave_model_usage` do `knowledge_fts` esta espelhado como Functional Requirement rastreavel na spec (FR-00X), dado que a spec (nao o plan) e o artefato formal consumido por `/create-tasks` e por auditorias futuras? [Rastreabilidade, Spec §Requirements] {auto} `[Gap]` — FR-001..FR-009 nao mencionam `knowledge_fts` nem a restricao de seguranca; ela existe apenas em `plan.md`, tornando-a invisivel para quem le so a spec.
- [x] CHK015 - O tratamento de escape SQL (`sql_escape`) e higiene de bytes (`strip_nul`) para o campo `model` (unico valor de origem externa desta feature) esta explicitamente exigido nas DUAS posicoes onde o valor aparece (coluna `model` e coluna `source_id`)? [Completude, Plan §Tratamento do nome do modelo] {auto} — Satisfeito: `plan.md` itens "1. Escape SQL — OBRIGATORIO" e "1.bis. Higiene de bytes" cobrem as duas posicoes explicitamente.
- [x] CHK016 - Esta clara a justificativa de por que o campo `model` NAO passa por `recall_scrub` (filtro de segredos), distinguindo campo estruturado de texto livre, evitando tanto fuga de dado quanto mutilacao indevida do identificador (requisito de fidelidade da string bruta, FR-001)? [Clareza, Consistencia, Plan §Filtro de segredos] {auto} — Satisfeito: `plan.md` item "2. Filtro de segredos — NAO se aplica" explica o criterio (campo estruturado, mesma classe de `event_type`) e a tensao com FR-001.

## Completude e Consistencia Geral

- [x] CHK017 - Todos os FRs (FR-001..FR-009) tem pelo menos um cenario associado, conforme validado pelo gate deterministico `requirement-coverage.sh`? [Cobertura] {auto} — Satisfeito: `requirement-coverage.sh docs/specs/otel-model-breakdown/spec.md` retornou `RESULT|...|requirements=9|covered=9|errors=0` (exit 0, zero findings).
- [x] CHK018 - A remocao de `otel_session_id` do escopo (decidida em Clarifications) esta refletida consistentemente em todos os artefatos, sem residuo de referencia a essa coluna como se fizesse parte do design final (plan/data-model)? [Consistencia] {auto} — Satisfeito: `grep -rn "otel_session_id" docs/specs/otel-model-breakdown/*.md` so retorna ocorrencias em `spec.md` (secao Clarifications, documentando a remocao); `plan.md` e `data-model.md` nao referenciam a coluna.
- [x] CHK019 - As Success Criteria (SC-001..SC-004) sao objetivamente mensuraveis contra corpus real, em vez de formulacoes vagas? [Mensurabilidade, Spec §Success Criteria] {auto} — Satisfeito: SC-001 exige batimento exato de soma contra `state.json`, SC-002 exige 100% das ondas sem OTel sem erro/zero linhas, SC-003 exige idempotencia do reindex, SC-004 exige suite 100% verde — todos verificaveis binariamente.
- [x] CHK020 - O plano justifica explicitamente por que a complexidade aceita (redundancia entre `otel_subagent_tokens` e as 4 colunas novas de parcela; `model` duplicado em `source_id`) e aceitavel e nao uma violacao de constitution? [Consistencia, Plan §Complexity Tracking] {auto} — Satisfeito: `plan.md §Complexity Tracking` lista as duas escolhas com "Por que" e "Alternativa rejeitada porque" para cada uma.
- [x] CHK021 - O escopo do bugfix separado do guard de invalidacao de delta (comparacao de `session_id` entre inicio/fim de onda) permanece de fato fora desta feature na priorizacao do backlog do mantenedor, mesmo com a investigacao empirica indicando que ele "nunca dispara" no ambiente observado — isso e aceitavel dado o risco residual de dado silenciosamente incorreto caso o guard falhe em disparar quando devesse? [Risco] {humano} — **RESOLVIDO (2026-07-28, mantenedor)**: SIM, permanece fora do escopo. Reconfirmado APOS a investigacao empirica (que so ficou disponivel depois da decisao original em `block-002`). Motivo: nao misturar correcao de coleta com migracao de schema. Permanece como `sug-002` priorizada na knowledge.db; a spec deve registrar o risco residual de subcontagem silenciosa como limitacao conhecida do dado ingerido.

## Notes

- Items `{auto}` foram resolvidos pelo agente lendo spec.md/plan.md/data-model.md/quickstart.md/research.md e citando a evidencia (`[x]` com fonte) ou marcando `[Gap]` quando a evidencia nao existe.
- Items `{humano}` ficam `[ ]` aguardando decisao do dono do produto — nao foram auto-marcados.
- Gate deterministico `requirement-coverage.sh` executado sobre `spec.md`: `requirements=9|covered=9|errors=0` (exit 0) — nenhum `[Gap]` adicional gerado por esse gate especificamente (todos os gaps acima vieram da leitura direta dos artefatos, nao do gate de cenarios).
- Marcar items concluidos com `[x]` conforme forem endereçados pelo dono do produto ou por `/create-tasks`.
